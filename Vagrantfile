# -*- mode: ruby -*-
# vi: set ft=ruby :

Vagrant::Config.run do |config|
  # All Vagrant configuration is done here. The most common configuration
  # options are documented and commented below. For a complete reference,
  # please see the online documentation at vagrantup.com.

  # Every Vagrant virtual environment requires a box to build off of.
  

  #This box is acquired from the s3 cloudbiolinux distribution. 
  #Run this command to get it : 

  #vagrant box add cloudbiolinux https://s3.amazonaws.com/cloudbiolinux/cbl_ubuntu_11_04_64bit_20111019.box
 
 #Now, we specify the box to be a version of the above 
 config.vm.box = "cloudbiolinux"

 


end
